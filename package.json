{
  "name": "gestor-proyectos-jeff",
  "version": "0.2.0",
  "description": "Aplicación de escritorio para gestionar proyectos, avances y documentos.",
  "main": "electron/main.js",
  "type": "commonjs",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "check": "node --check electron/main.js && node --check electron/preload.js && node --check electron/services/database-service.js && node --check database/repositories/proyectos-repository.js && node --check database/repositories/tipos-repository.js && node --check database/repositories/archivos-repository.js && node --check src/app.js"
  },
  "keywords": [
    "electron",
    "proyectos",
    "gestion",
    "documentos",
    "sqlite"
  ],
  "author": "Jefferson Villarreal",
  "license": "MIT",
  "devDependencies": {
    "electron": "latest"
  },
  "engines": {
    "node": ">=22.13"
  }
}
